import { SafeAreaView, StatusBar, Text } from "react-native";

const Home = () => {
  return (
    <SafeAreaView>
      <StatusBar />
      <Text>Hello World</Text>
    </SafeAreaView>
  );
};

export default Home;
